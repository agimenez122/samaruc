MODULE uno;
(* programa vacio *)
  CONST constante1 = 5;
	constante2 = TRUE;
  	constante3 = FALSE;

  TYPE  miTipo1 = ARRAY [1..6] OF BOOLEAN;
  	miTipo2 = ARRAY [10..constante1] OF INTEGER;

  VAR   variable1,variable2 : INTEGER;
 	variable3,variable4 : BOOLEAN;
  	variable5,variable6,variable7 : miTipo1;

  PROCEDURE procedimiento ( v,x : INTEGER;
			    y : BOOLEAN);
	VAR aux, index : INTEGER;
  BEGIN
	IF 4=5 THEN
		aux := index;
	FI;
  FI procedimiento;

  PROCEDURE funcion ():BOOLEAN;
        VAR aux, index : INTEGER;
	PROCEDURE vacio;
	BEGIN
	FI vacio;
  BEGIN 
	FOR i:=5 TO final - i 
	DO
		RETURN TRUE;
	FI;
        aux := index;
	RETURN aux;
  FI funcion;

BEGIN
	WRITELN;
	variable0 := pepe;
	variable1 := (3 * 4 - 2);
	variable2 := (3-5) * (4 - 2);
	variable3 := (3 - 2 -1);
	variable4 := variable3 - (2 - 1);
	variable5 := (funcion() * 34 - funcion2(5,6,TRUE));
	variable6 := numero - masnumero * ((((aleluya))));

	variable5 := (5=5);
	variable1 := (TRUE OR (b > c)); 
	variable2 := NOT ( a > b) OR (9 > d);
	variable3 := (a = b) OR ((b = c)) OR (c = d);
	variable4 := (a = b) OR ((b = c) OR (c = d));
	variable5 := 4>6;
	variable6 := NOT TRUE;
	variable7 := NOT ( a OR b);
	variable9 := (a OR TRUE);
	variable10 := (0-4)=(2*mifuncion(TRUE));
	variable11 := v[index] OR NOT mfuncion(4, 5, valor);
	variable12 := NOT NOT TRUE;

	
	variable4 := (45);
	v[5] := (TRUE);
	variable3 := (variable2);
	variable5 := (v[6]);
	
	IF uno THEN
		variable := 45;
		IF 4=5 THEN
			variable := 34;
		ELSE
			variable := 43;
			funcion1;
			funcion2(V[4], 5, variable, TRUE);
			funcion3();
			IF mfuncion(TRUE) THEN
				FOR index := 4 TO 5*2-corrector
				DO
					WRITESTRING("¿La vida es triste? Coño, una ñ");
				FI;
			FI;
		FI;
		v[45] := ((4>1));	
	FI;
	variable := funcion2(5, 4-3);
	IF dos THEN
		mifuncion();
	ELSE
		mifuncion2();
	FI;


	FOR indice := 0 TO resta(5-3)
	DO
		v[45] := 4>1;
	FI; 
	WRITELN;
	WRITEINT(24*3);
	WRITESTRING("Hola Mundo!");
FI uno;	
  

