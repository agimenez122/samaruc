MODULE uno;
(* programa vacio *)
  CONST constante1 = 5;
	constante2 = TRUE;
  	constante3 = FALSE;

  TYPE  miTipo1 = ARRAY [1..6] OF BOOLEAN;
  	miTipo2 = ARRAY [10..constante1] OF INTEGER;

  VAR   variable1,variable2 : INTEGER;
 	variable3,variable4 : BOOLEAN;
  	variable5,variable6,variable7 : miTipo1;

  PROCEDURE procedimiento ( v,x : INTEGER;
			    y : BOOLEAN);
	VAR aux, index : INTEGER;
  BEGIN
	aux := index;
  FI procedimiento;

 PROCEDURE funcion ():BOOLEAN;
        VAR aux, index : INTEGER;
  BEGIN 
        aux := index;
(*	RETURN aux;   *)
  FI funcion;


  BEGIN

	variable1 := 3 * 4 - 2;
	variable2 := 3 * (4 - 2);  
	variable3 := (3 - 2 -1);
	variable4 := variable3 - (2 - 1); 

	variable1 := (TRUE OR (b > c)); 
	variable2 := NOT ( a > b) OR (9 > d);
	variable3 := (a = b) OR (b = c) OR (c = d);
	variable4 := (a = b) OR ((b = c) OR (c = d));
	variable5 := 4>6;
	variable5 := (5=5);
	variable6 := NOT TRUE;
	variable7 := NOT ( a OR b);
	variable9 := (a OR TRUE);

	
	variable4 := (45);
	v[5] := (TRUE);
	variable3 := (variable2);
	variable5 := (v[6]);
	
	IF v[5] THEN
		variable := 45;
		IF 4=5 THEN
			variable := 34;
		ELSE
			variable := 43;
			funcion1;
			funcion2(V[4], 5, variable, TRUE);
			funcion3();

		FI;
	FI;
	
	variable := funcion2(5, 4-3);

	FOR indice := 0 TO 34
	DO
		v[45] := 4>1;
	FI;

	WRITELN;
	WRITEINT(24*3);
	WRITESTRING("Hola Mundo!"); 
	
  FI uno;
